spring:
  datasource:
    url: jdbc:h2:mem:credit_application_system_db
    username: johnny
    password:
  jpa:
    show-sql?: true
    #hibernate:
      # Setting removed after insert "Properties: hibernate.format_sql: true"
      #ddl-auto: create-drop
    properties:
      hibernate.format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false